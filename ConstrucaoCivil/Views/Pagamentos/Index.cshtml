@model IEnumerable<ConstrucaoCivil.Models.tblPagamento>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.quantia)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.data)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tipo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.createAt)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.updatedAt)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tblFuncionario.nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tblObra.nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.quantia)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.data)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tipo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.createAt)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.updatedAt)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tblFuncionario.nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tblObra.nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_pagam }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_pagam }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_pagam })
        </td>
    </tr>
}

</table>
